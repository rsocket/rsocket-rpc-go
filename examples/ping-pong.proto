// protoc --go_out=plugins=rrpc:./ping-pong ./ping-pong.proto
syntax = "proto3";

package ping_pong;

message Ping {
    string ball = 1;
}

message Pong {
    string ball = 1;
}

service PingPong {
    rpc ping (Ping) returns (Pong) {
    }
}
